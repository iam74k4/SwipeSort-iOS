name: Release

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:
    inputs:
      version:
        description: 'Version number (e.g., 1.0.0)'
        required: true
        type: string

permissions:
  contents: write

env:
  SCHEME: SwipeSort
  PROJECT: SwipeSort.xcodeproj
  ARCHIVE_PATH: build/SwipeSort.xcarchive
  EXPORT_PATH: build/export
  XCODE_VERSION: '26.2'

jobs:
  build-and-release:
    name: Build and Release
    runs-on: macos-15
    timeout-minutes: 60
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v6
        with:
          fetch-depth: 0
      
      - name: Get version and build number
        id: version
        run: |
          if [ "${{ github.event_name }}" == "workflow_dispatch" ]; then
            MARKETING_VERSION="${{ github.event.inputs.version }}"
          else
            MARKETING_VERSION=${GITHUB_REF#refs/tags/v}
          fi
          # スクリプトを使用してビルド番号を取得
          chmod +x scripts/version.sh
          source scripts/version.sh
          # タグから取得したバージョンを使用（スクリプトの結果を上書き）
          export MARKETING_VERSION="$MARKETING_VERSION"
          echo "marketing_version=$MARKETING_VERSION" >> $GITHUB_OUTPUT
          echo "current_project_version=$CURRENT_PROJECT_VERSION" >> $GITHUB_OUTPUT
          echo "Using version: $MARKETING_VERSION (build: $CURRENT_PROJECT_VERSION)"
      
      - name: Setup Xcode
        uses: maxim-lobanov/setup-xcode@v1.6.0
        with:
          xcode-version: ${{ env.XCODE_VERSION }}
      
      - name: Show environment
        run: |
          echo "Xcode version:"
          xcodebuild -version
          echo "Swift version:"
          swift --version
          echo "Available SDKs:"
          xcodebuild -showsdks
      
      - name: Cache DerivedData
        uses: actions/cache@v5
        with:
          path: ~/Library/Developer/Xcode/DerivedData
          key: ${{ runner.os }}-xcode-${{ env.XCODE_VERSION }}-release-${{ hashFiles('**/*.xcodeproj/project.pbxproj') }}
          restore-keys: |
            ${{ runner.os }}-xcode-${{ env.XCODE_VERSION }}-release-
            ${{ runner.os }}-xcode-${{ env.XCODE_VERSION }}-
      
      - name: Generate CHANGELOG
        id: changelog
        run: |
          if [ "${{ github.event_name }}" == "workflow_dispatch" ]; then
            CHANGELOG="## Release ${{ steps.version.outputs.marketing_version }}

          ### Changes
          - See commit history for details"
          else
            PREV_TAG=$(git describe --tags --abbrev=0 HEAD^ 2>/dev/null || echo "")
            if [ -z "$PREV_TAG" ]; then
              CHANGELOG="## Release ${{ steps.version.outputs.marketing_version }}

          ### Initial Release"
            else
              CHANGELOG="## Release ${{ steps.version.outputs.marketing_version }}

          ### Changes since $PREV_TAG

          $(git log ${PREV_TAG}..HEAD --pretty=format:'- %s' --no-merges)"
            fi
          fi
          echo "changelog<<EOF" >> $GITHUB_OUTPUT
          echo "$CHANGELOG" >> $GITHUB_OUTPUT
          echo "EOF" >> $GITHUB_OUTPUT
      
      - name: Build archive
        run: |
          mkdir -p build
          xcodebuild archive \
            -project $PROJECT \
            -scheme $SCHEME \
            -archivePath $ARCHIVE_PATH \
            -destination 'generic/platform=iOS' \
            -configuration Release \
            MARKETING_VERSION=${{ steps.version.outputs.marketing_version }} \
            CURRENT_PROJECT_VERSION=${{ steps.version.outputs.current_project_version }} \
            CODE_SIGN_IDENTITY="" \
            CODE_SIGNING_REQUIRED=NO \
            CODE_SIGNING_ALLOWED=NO \
            COMPILER_INDEX_STORE_ENABLE=NO \
            SWIFT_OPTIMIZATION_LEVEL=-O \
            -derivedDataPath ~/Library/Developer/Xcode/DerivedData
      
      - name: Create ExportOptions.plist
        run: |
          cat > ExportOptions.plist << 'EOF'
          <?xml version="1.0" encoding="UTF-8"?>
          <!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
          <plist version="1.0">
          <dict>
              <key>method</key>
              <string>development</string>
              <key>compileBitcode</key>
              <false/>
          </dict>
          </plist>
          EOF
      
      - name: Export IPA
        continue-on-error: true
        run: |
          mkdir -p $EXPORT_PATH
          xcodebuild -exportArchive \
            -archivePath $ARCHIVE_PATH \
            -exportPath $EXPORT_PATH \
            -exportOptionsPlist ExportOptions.plist \
            || echo "Export failed (signing required for production builds)"
      
      - name: Upload build artifacts
        uses: actions/upload-artifact@v6
        with:
          name: SwipeSort-${{ steps.version.outputs.marketing_version }}
          path: |
            ${{ env.ARCHIVE_PATH }}
            ${{ env.EXPORT_PATH }}/*.ipa
          retention-days: 90
          if-no-files-found: warn
      
      - name: Create Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ github.event_name == 'workflow_dispatch' && format('v{0}', steps.version.outputs.marketing_version) || github.ref_name }}
          name: Release ${{ steps.version.outputs.marketing_version }}
          body: ${{ steps.changelog.outputs.changelog }}
          draft: false
          prerelease: false
          files: |
            ${{ env.ARCHIVE_PATH }}/Products/Applications/${{ env.SCHEME }}.app
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
